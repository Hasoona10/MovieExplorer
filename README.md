# Movie Explorer

CPSC 349 — React app.

## Run locally

```bash
npm install
```

Use api key from movie db site. Add `VITE_TMDB_API_KEY=` in a `.env` file in this folder.

```bash
npm run dev
```

## Build

```bash
npm run build
```
